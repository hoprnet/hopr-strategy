[package]
authors      = ["HOPR Association <tech@hoprnet.org>"]
categories   = ["network-programming"]
description  = "Contains implementations of different HOPR strategies"
edition      = "2024"
homepage     = "https://hoprnet.org/"
include      = ["Cargo.toml", "LICENSE", "README.md", "src/**/*.rs"]
keywords     = ["channels", "hopr", "strategy"]
license      = "GPL-3.0-only"
name         = "hopr-strategy"
readme       = "README.md"
repository   = "https://github.com/hoprnet/hopr-strategy"
rust-version = "1.97"
version      = "5.0.0"

[lib]
crate-type = ["rlib"]

[features]
default       = ["runtime-tokio"]
runtime-tokio = ["hopr-utils/runtime-tokio"]
telemetry     = ["hopr-api/types-telemetry"]
## Exposes `crate::testing` node adapters to downstream test crates.
testing = ["hopr-utils/testing-blokli"]
## Strategy features — each enables one strategy implementation.
strategy-auto-funding      = []
strategy-auto-redeeming    = []
strategy-channel-lifecycle = []
strategy-closure-finalizer = []
# The PIX strategy engine, without a deposit pool.
strategy-pix = [
  "dep:backon",
  "dep:chacha20poly1305",
  "dep:redb",
  "dep:scrypt",
  "dep:subtle",
  "dep:tokio",
]
# `NonAnonymousDepositPool`, keyed on `EthDepositKey` (Ethereum deposit addresses).
#
# **For testing and development only.** The pool settles with plain, fully visible on-chain
# transfers, so every deposit and sweep is linkable — it offers no anonymity at all.
strategy-pix-test = ["dep:hopr-chain-connector", "strategy-pix"]
# `CurvyDepositPool`, keyed on `BjjKeypair` (Baby JubJub deposit addresses): settlement through
# the Curvy shielded pool, with per-SSA stealth scan identities for private note discovery.
#
# Pair with `hopr-crypto-packet/pix-bjj`, which is the default, so a consumer enabling this
# feature and nothing else already agrees.
#
# Pulls in the Curvy SDK (`curvy-sdk`), the Curvy stealth-address and note primitives
# (`curvy-core`), and `blokli-client`'s `curvy` query surface for note discovery. The SDK needs
# the Curvy proving artifacts on disk at runtime (`CURVY_ZK_KEYS_DIR`) — see the README.
strategy-pix-curvy = [
  "blokli-client/curvy",
  "dep:babyjubjub-ec",
  "dep:blokli-client",
  "dep:const-hex",
  "dep:curvy-abi",
  "dep:curvy-chain-api",
  "dep:curvy-chain-blokli",
  "dep:curvy-core",
  "dep:curvy-sdk",
  "dep:curvy-types",
  "dep:reqwest",
  "dep:serde_json",
  "dep:zeroize",
  "hopr-api/types-curvy",
  "strategy-pix",
]

[dependencies]
anyhow              = { workspace = true }
async-trait         = { workspace = true }
bytesize            = { workspace = true }
dashmap             = { workspace = true, features = ["inline"] }
futures             = { workspace = true }
futures-concurrency = { workspace = true }
futures-time        = { workspace = true }
humantime-serde     = { workspace = true }
lazy_static         = { workspace = true }
serde               = { workspace = true, features = ["derive"] }
serde_with          = { workspace = true }
smart-default       = { workspace = true }
statrs              = { workspace = true }
thiserror           = { workspace = true }
tracing             = { workspace = true }
validator           = { workspace = true, features = ["derive"] }

hopr-api = { workspace = true, features = [
  "node",
  "serde",
  "types-chain",
  "types-internal",
] }
hopr-chain-connector = { workspace = true, optional = true }
hopr-utils = { workspace = true, default-features = false }
moka = { workspace = true, features = ["sync"] }
parking_lot = { workspace = true }

# Optional deps enabled by the `strategy-pix-curvy` feature. `blokli-client`'s `testing`
# feature is deliberately not enabled here — a production curvy build must not inherit the test
# emulator; the crate's own tests get it through the dev-dependency below.
blokli-client = { workspace = true, optional = true }
serde_json = { workspace = true, optional = true }
tokio = { workspace = true, optional = true, features = [
  "macros",
  "rt",
  "sync",
  "time",
] }

# PIX (non-anonymous) strategy support — enabled via the `strategy-pix` feature.
backon = { workspace = true, features = [
  "futures-timer-sleep",
], optional = true }
chacha20poly1305 = { workspace = true, optional = true }
redb = { workspace = true, optional = true }
scrypt = { workspace = true, optional = true }
subtle = { workspace = true, optional = true }

# Curvy deposit pool — enabled via the `strategy-pix-curvy` feature.
babyjubjub-ec = { workspace = true, optional = true }
const-hex     = { workspace = true, optional = true }
curvy-abi     = { workspace = true, optional = true }
# The relayer's HTTP surface. Already in the graph via `blokli-client` and `alloy`, so this
# declares a dependency rather than adding one.
curvy-chain-api = { workspace = true, optional = true }
curvy-chain-blokli = { workspace = true, optional = true }
curvy-core = { workspace = true, optional = true }
curvy-sdk = { workspace = true, optional = true }
curvy-types = { workspace = true, optional = true }
reqwest = { workspace = true, optional = true, default-features = false, features = [
  "json",
  "rustls",
] }
zeroize = { workspace = true, optional = true }

[dev-dependencies]
blokli-client = { workspace = true, features = ["testing"] }
chrono = { workspace = true, default-features = false, features = [
  "clock",
  "std",
] }
hex-literal = { workspace = true }
hopr-api = { workspace = true, features = ["types-random"] }
hopr-utils = { workspace = true, features = [
  "runtime-tokio",
  "testing-blokli",
] }
insta = { workspace = true, features = ["redactions", "yaml"] }
mockall = { workspace = true }
rstest = { workspace = true }
serde_json = { workspace = true }
tempfile = { workspace = true }
test-log = { workspace = true, features = ["trace"] }
tokio = { workspace = true, features = [
  "macros",
  "rt-multi-thread",
  "net",
  "io-util",
  "test-util",
  "tracing",
] }

[workspace]
members  = ["tests/integration"]
resolver = "3"

[workspace.dependencies]
anyhow           = "1.0.104"
async-trait      = "0.1.92"
babyjubjub-ec    = { version = "0.4.0", features = ["arithmetic"] }
backon           = { version = "1.6.0", default-features = false }
blokli-client    = "0.33.1"
bytesize         = { version = "2.7.0", features = ["serde"] }
chacha20poly1305 = "0.11.0"
chrono           = { version = "0.4.45", default-features = false }
const-hex        = "1.19.1"
# The Curvy crates are pre-release and change API between release candidates; `curvy-sdk` pins
# `curvy-core` exactly (rc.6 is the last `curvy-core`), so the rs-sdk crates must name the same
# candidate and move together.
curvy-abi            = "=0.1.0-rc.7"
curvy-chain-api      = "=0.1.0-rc.7"
curvy-chain-blokli   = "=0.1.0-rc.7"
curvy-core           = "=0.1.0-rc.6"
curvy-sdk            = "=0.1.0-rc.7"
curvy-types          = "=0.1.0-rc.7"
dashmap              = "6.2.1"
futures              = "0.3.34"
futures-concurrency  = "7.7.1"
futures-time         = "3.1.0"
hex-literal          = "1.1.0"
hopr-api             = "4.3.0"
hopr-chain-connector = "0.26.0"
hopr-strategy        = { path = "." }
hopr-utils           = { package = "hopr-utilities", version = "0.14.0", default-features = false }
humantime-serde      = "1.1.1"
insta                = "1.48.0"
lazy_static          = "1.5.0"
mockall              = "0.15.0"
moka                 = "0.12.16"
parking_lot          = "0.12.5"
redb                 = "4.2.0"
reqwest              = { version = "0.13.4", default-features = false }
rstest               = "0.27.0"
scrypt               = "0.12.0"
serde                = "1.0.229"
serde_json           = "1.0.151"
serde_with           = "3.23.0"
smart-default        = "0.7.1"
statrs               = "0.19.1"
subtle               = "2.6.1"
tempfile             = "3.27.0"
test-log             = "0.2.21"
thiserror            = "2.0.20"
tokio                = "1.53.1"
tracing              = "0.1.44"
validator            = "0.21.0"
zeroize              = "1.8.1"

[package.metadata.cargo-machete]
ignored = ["humantime-serde"]
